import SupportTickets from "@/components/admin/SupportTickets";

export const metadata = { title: "Support Tickets — RideX Admin" };

export default function SupportTicketsPage() {
  return <SupportTickets />;
}
